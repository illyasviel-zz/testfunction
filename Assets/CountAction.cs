using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class CountAction : MonoBehaviour {

  //  Text text;
  //  RecordAction recordAction;
  //  CreatePlayer createPlayer;
  //  float nextTime = 0;
  //  ArrayList record = new ArrayList();
    // Use this for initialization
	void Start () {
    /*    text = GetComponent<Text>();
        recordAction = GameObject.Find("GameRecord").GetComponent<RecordAction>();
        createPlayer = Camera.main.GetComponent<CreatePlayer>();
        record = recordAction.List;

        //head = recordAction.returnhead;*/
    }
	
	// Update is called once per frame
	void Update () {
        //text.text = (recordAction.actionPosRecord).ToString();
        //text.text = (createPlayer.touchPoint).ToString();
       /* foreach(int i in recordAction.List)
        {
            text.text = recordAction.List[i].ToString();
            
        }*/
    }
}
