using UnityEngine;
using System.Collections;

public class PlayerAttack : MonoBehaviour {

    /*Generator generateAtt;
    PlayerMovement playerMov;
    float minDis;
    GameObject enemy;
    Animator ani;

    // Use this for initialization
    void Awake () {
        generateAtt = GetComponentInChildren<Generator>();
        minDis = generateAtt.range;
        playerMov = GetComponent<PlayerMovement>();
        ani = GetComponent<Animator>();
    }
	
	// Update is called once per frame
	void Update () {
        enemy = playerMov.GetNearestEnemy(GameObject.FindGameObjectsWithTag("enemy"));
        if(Vector3.Distance(this.transform.position, 
            enemy.transform.position) <= minDis)
        {
            ani.SetBool("isAttrack", true);
        }
    }*/
}
